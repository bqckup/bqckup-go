site:
  name: example
  enabled: true
  # For incremental backups, uncomment these fields and set a strong password.
  # Keep this file at mode 0600. Full mode is the default.
  # backup_mode: incremental
  # incremental:
  #   password: replace-with-a-strong-repository-password
  sources:
    files:
      include:
        - /srv/example/data
      exclude:
        - /srv/example/data/cache
      follow_symlinks: false
    databases:
      - name: mysql-example
        enabled: false
        engine: mysql
      - name: postgres-example
        enabled: false
        engine: postgres
  destinations:
    - storage: local-primary
  policy:
    minimum_interval: 24h
    keep_last: 7
