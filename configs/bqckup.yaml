server_id: 127.0.0.1

app:
  state_database: data/bqckup.db
  temporary_directory: tmp
  lock_directory: locks
  log_level: info
  # log_file: /var/log/bqckup/bqckup.log

# Keep per-event notifications commented out unless you want immediate backup
# alerts. The default operational mode is scheduled daily/monthly summaries.
# notifications:
#   channels:
#     email:
#       type: smtp
#       host: <smtp-host>
#       port: 587
#       username: <smtp-user>
#       password: <smtp-password>
#       from: <sender-address>
#       to:
#         - <recipient-address>
#     webhook:
#       type: webhook
#       url: <webhook-url>
#     discord:
#       type: discord
#       webhook_url: <discord-webhook-url>
#   routes:
#     - events: [backup_failed]
#       channels: [email, discord, webhook]

# reports:
#   daily:
#     enabled: true
#     timezone: UTC
#     schedule:
#       time: "08:00"
#     notification_route: daily-report-route
#     include_empty_days: false
#   monthly:
#     enabled: true
#     timezone: UTC
#     schedule:
#       day_of_month: 1
#       time: "08:00"
#     notification_route: monthly-report-route
#     include_empty_days: false
