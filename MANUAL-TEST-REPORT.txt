BQCKUP-GO MANUAL TEST REPORT
============================

Date/time zone : 2026-08-23 Asia/Makassar
Branch         : 7-incremental-backup
Base commit    : 34e152f
Changes tested : current working tree
Go             : go1.26.5 linux/amd64
Platform       : Kali Linux 2026.2, linux/amd64
CGO/GCC        : enabled / available

Security and isolation
----------------------

- Source, state database, locks, local storage, and runtime config were placed
  below /tmp/bqckup-manual-smoke.
- The credential-bearing storage file had mode 0600.
- Storage credentials are intentionally REDACTED from this report and are not
  present in the repository diff.
- Live objects used this isolated prefix:
  manual-smoke/7a4aae17-0f67-4ac8-a317-7be187c0d550
- The live integration contract tests used unique child prefixes and cleaned
  those child objects after completion.

Automated baseline
------------------

[PASS] Formatting, vet, race-enabled unit/integration suite, and CLI build.

Command:
  make verify

Result:
  - gofmt check passed
  - go vet ./... passed
  - go test -race ./... passed, including SQLite/history/app/CLI
  - go build ./cmd/bqckup passed

[PASS] Documentation and diff checks.

Commands:
  bash scripts/check-docs.sh
  git diff --check

Manual CLI matrix
-----------------

[PASS] CLI help and command discovery.

Commands:
  bqckup --help
  bqckup backup --help
  bqckup backup run --help
  bqckup backup unlock --help
  bqckup config --help
  bqckup history --help
  bqckup doctor --help

[PASS] Version and shell completion.

Commands:
  bqckup version
  bqckup completion bash > /tmp/bqckup-completion.bash

Observed:
  - version returned "bqckup dev (unknown)" for a local development build
  - non-empty Bash completion script was generated

[PASS] Config initialization and no-overwrite behavior.

Commands:
  bqckup --config-dir /tmp/bqckup-manual-smoke/init-config init
  bqckup --config-dir /tmp/bqckup-manual-smoke/init-config init

Observed:
  - first init created bqckup.yaml, config/storages.yaml, sites/example.yaml
  - all generated files had mode 0600
  - second init was rejected with exit code 2 and did not overwrite files

[PASS] Strict schema-v2 validation in text and JSON formats.

Commands:
  bqckup --config-dir /tmp/bqckup-manual-smoke/config config validate
  bqckup --config-dir /tmp/bqckup-manual-smoke/config --output json config validate

Observed:
  - five sites and two storages validated successfully

[PASS] Doctor healthy path, JSON output, and site filtering.

Commands:
  MANUAL_REPO_PASSWORD='<private>' bqckup --config-dir /tmp/bqckup-manual-smoke/config doctor
  MANUAL_REPO_PASSWORD='<private>' bqckup --config-dir /tmp/bqckup-manual-smoke/config --output json doctor
  PATH=/tmp/bqckup-manual-smoke/fake-bin:$PATH bqckup --config-dir /tmp/bqckup-manual-smoke/config doctor --site database-exporters

Observed:
  - config/temp/lock/state checks passed
  - native incremental engine and password environment checks passed
  - disposable mysqldump and pg_dump executables were detected

[PASS] Doctor missing-secret and missing-binary failure paths.

Commands:
  unset MANUAL_REPO_PASSWORD
  bqckup --config-dir /tmp/bqckup-manual-smoke/config --output json doctor --site incremental-s3
  bqckup --config-dir /tmp/bqckup-manual-smoke/config doctor --site database-exporters

Observed:
  - both dependency failures returned exit code 3
  - missing incremental password was reported without exposing its value

[PASS] Backup site listing and history in text/JSON.

Commands:
  bqckup --config-dir /tmp/bqckup-manual-smoke/config backup list
  bqckup --config-dir /tmp/bqckup-manual-smoke/config --output json backup list
  bqckup --config-dir /tmp/bqckup-manual-smoke/config history list
  bqckup --config-dir /tmp/bqckup-manual-smoke/config --output json history list

Observed:
  - site mode/destination metadata was listed
  - successful runs and stored artifacts were persisted in SQLite history

[PASS] Full local file backup, excludes, checksum, and permissions.

Command:
  bqckup --config-dir /tmp/bqckup-manual-smoke/config backup run full-local

Observed:
  - files.tar.gz was stored with mode 0600
  - tar contained source/keep.txt
  - *.tmp and cache/** contents were absent
  - checksum/size were recorded in history

[PASS] Minimum interval skip and forced run.

Commands:
  bqckup --config-dir /tmp/bqckup-manual-smoke/config backup run full-local
  bqckup --config-dir /tmp/bqckup-manual-smoke/config backup run full-local
  bqckup --config-dir /tmp/bqckup-manual-smoke/config --output json backup run full-local --force

Observed:
  - second ordinary run returned "skipped (minimum_interval)"
  - --force created a new successful run

[PASS] Non-blocking site lock.

Command outline:
  flock -x /tmp/bqckup-manual-smoke/locks/full-local.lock -c 'sleep 3' &
  bqckup --config-dir /tmp/bqckup-manual-smoke/config backup run full-local --force

Observed:
  - command returned "skipped (already_running)" with exit code 0

[PASS] Full-backup retention.

Command:
  bqckup --config-dir /tmp/bqckup-manual-smoke/config backup run full-local --force

Observed:
  - after three successful backup sets with keep_last=2, exactly two local
    backup-set directories remained

[PASS] Full backup with all-required local + live S3 destinations.

Command:
  bqckup --config-dir /tmp/bqckup-manual-smoke/config backup run full-multi

Observed:
  - run succeeded
  - history contained one local and one remote artifact
  - both artifacts recorded identical size and SHA-256

[PASS] Native incremental local backup and clean unlock.

Commands:
  MANUAL_REPO_PASSWORD='<private>' bqckup --config-dir /tmp/bqckup-manual-smoke/config backup run incremental-local
  MANUAL_REPO_PASSWORD='<private>' bqckup --config-dir /tmp/bqckup-manual-smoke/config backup unlock incremental-local
  MANUAL_REPO_PASSWORD='<private>' bqckup --config-dir /tmp/bqckup-manual-smoke/config --output json backup run incremental-local --force

Observed:
  - two versions backed up successfully without any external Restic binary
  - keep_last=1 left exactly one snapshot
  - prune reclaimed 778 bytes
  - clean unlock completed successfully

[PASS] Native incremental live S3 backup, lock, listing, and retention.

Commands:
  MANUAL_REPO_PASSWORD='<private>' bqckup --config-dir /tmp/bqckup-manual-smoke/config backup run incremental-s3
  MANUAL_REPO_PASSWORD='<private>' bqckup --config-dir /tmp/bqckup-manual-smoke/config backup unlock incremental-s3
  MANUAL_REPO_PASSWORD='<private>' bqckup --config-dir /tmp/bqckup-manual-smoke/config --output json backup run incremental-s3 --force

Observed:
  - repository init/open, two encrypted snapshots, lock/unlock, and retention
    completed successfully against the selected S3-compatible storage
  - keep_last=1 prune reclaimed 811 bytes

[PASS] Live S3 storage contract and disposable incremental repository.

Command:
  BQCKUP_S3_INTEGRATION_CONFIG=/tmp/bqckup-manual-smoke/config \
  BQCKUP_S3_INTEGRATION_STORAGE=remote-testing \
  go test -tags=integration ./internal/storage/s3compat \
    ./internal/engine/restic/facade \
    -run 'TestDisposableS3CompatibleStorage|TestDisposableIncrementalBackupS3Compatible' \
    -count=1 -v

Observed:
  - Put/list/delete/checksum storage test passed in 2.21 seconds
  - two-snapshot incremental/exclude/list/retention/cleanup test passed in
    35.02 seconds

[PASS] MySQL and PostgreSQL exporter process wiring with dummy executables.

Command:
  PATH=/tmp/bqckup-manual-smoke/fake-bin:$PATH \
  bqckup --config-dir /tmp/bqckup-manual-smoke/config backup run database-exporters

Observed:
  - one file archive and two .sql.gz artifacts were stored
  - all artifacts had mode 0600
  - decompressed dummy SQL matched the executable output
  - three artifacts were recorded in history
  - passwords were passed through environment contracts, not CLI arguments

Not executed / intentionally unavailable
----------------------------------------

[PASS] Real MariaDB server dump with a disposable local database.

Commands:
  bqckup doctor --site mysql-test
  bqckup backup run mysql-test --force
  bqckup history list --site mysql-test
  gzip -t <private-local-backup>/databases/bqckup-test.sql.gz

Observed:
  - MariaDB 11.8.8 listened only on 127.0.0.1:3306
  - a dedicated least-privilege backup user exported the disposable database
  - the compressed SQL contained the expected table and dummy row
  - the file archive and SQL dump were mode 0600 and recorded in history
  - history text output showed a readable local date/time with zone, duration,
    artifact count, total size, and run ID

[NOT RUN] Real PostgreSQL server dump.
Reason: no disposable PostgreSQL server and credentials were supplied.
Covered instead by race-enabled unit tests and the disposable process-wiring
manual test above.

[NOT RUN] Official Restic compatibility oracle.
Reason: the official Restic binary is not installed. It is test-only and is
not required by runtime. Native local and live S3 repository flows passed.

[NOT IMPLEMENTED] Restore command.
Reason: restore remains a documented future phase, so no CLI restore behavior
exists to test.

[OUT OF SCOPE] Web UI, authentication, notifications, scheduler, reporting,
and webhooks. The architecture explicitly defines the project as CLI-only.

Final result
------------

All currently implemented CLI, local storage, S3-compatible storage, history,
retention, locking, full-backup, native incremental-backup, and real MariaDB
export behaviors that were available in this environment passed. PostgreSQL
server connectivity and the optional official-Restic oracle are the only
executable integrations not run, for the reasons recorded above.
